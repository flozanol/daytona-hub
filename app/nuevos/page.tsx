import { AutosNuevosWrapper } from '../../components/nuevos/AutosNuevosWrapper';

export const metadata = { title: 'Autos Nuevos — Daytona BI' };

export default function NuevosPage() {
  return <AutosNuevosWrapper />;
}
